# RIMS Desktop Environment Variables Template
# Copy this file to .env.development or .env.production and fill in the values

# Environment Mode (set automatically by npm scripts, but can be overridden)
# NODE_ENV=development
# NODE_ENV=production

# Database Configuration
# Note: Database path is automatically resolved based on NODE_ENV
# Development: data/dev/rims.db
# Production: userData/rims.db
# This variable is kept for backward compatibility but is optional
# DATABASE_PATH=./data/dev/rims.db  (development)
# DATABASE_PATH=                    (production - auto-resolved)

# Electron Start URL (optional)
# Leave empty for default behavior
# ELECTRON_START_URL=

# Sync Service Configuration
# URL untuk sync service (jika menggunakan sync service terpisah)
# Default: http://localhost:3001
SYNC_SERVICE_URL=http://localhost:3001

# Database Configuration (automatic)
# Database path is automatically resolved based on NODE_ENV
# Development: data/dev/rims.db
# Production: userData/rims.db
# No manual configuration needed

# Logging Configuration
# Log level is controlled by NODE_ENV
# Development: logs to console and file
# Production: logs to file only
# No manual configuration needed

# Application Settings
# Most settings are stored in settings.json (separate for dev/prod)
# No environment variables needed for basic settings
